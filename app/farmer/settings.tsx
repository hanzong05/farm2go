import React from 'react';
import Settings from '../../components/Settings';

export default function FarmerSettingsScreen() {
  return <Settings userType="farmer" currentRoute="/farmer/settings" />;
}